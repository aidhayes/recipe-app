import { NavLinkProps } from 'react-bootstrap';
import { BsPrefixRefForwardingComponent } from 'react-bootstrap/helpers';
import { SelectCallback } from "@restart/ui/types";
export interface SidebarMenuNavLinkProps extends Omit<NavLinkProps, 'onSelect'> {
    onSelect?: SelectCallback;
}
declare const SidebarMenuNavLink: BsPrefixRefForwardingComponent<'a', SidebarMenuNavLinkProps>;
export default SidebarMenuNavLink;
